#include "main.h"

/**
 * _abs - return the absolute value of a number
 * @n: number to check
 * Author-EngineerPhoenix
 * Return: positive value of n
 */
int _abs(int n)
{
	return ((n < 0) ? -1 * n : n);
}
